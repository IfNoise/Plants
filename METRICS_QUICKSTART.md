# Быстрый старт - MetricsChart

Компонент для визуализации метрик Victoria Metrics интегрирован в карточки ирригаторов.

## 🚀 Использование

### В UI

1. Откройте Dashboard с устройствами
2. Найдите карточку ирригатора
3. Нажмите кнопку с иконкой графика 📊 (ShowChart)
4. Откроется диалог с графиком состояния клапана

### Настройки графика

В диалоге доступны:

- **Период**: от 5 минут до 30 дней или произвольный
- **Автообновление**: от 5 секунд до 5 минут
- **Масштаб**: автоматический или ручной (min/max Y)
- **Запрос**: редактируется прямо в UI

## ⚙️ Конфигурация

### Backend (автоматически настроено)

Victoria Metrics URL: `http://ddweed.org:8428`

Файлы:

- `config/default.json` - разработка
- `config/production.json` - продакшен

Для переопределения используйте переменную окружения:

```bash
export VICTORIA_METRICS_URL=http://your-host:8428
```

### API Endpoints

- `GET /api/v1/query_range` - временные ряды
- `GET /api/v1/query` - мгновенные запросы
- `GET /api/v1/health` - проверка доступности

## 🧪 Тестирование

```bash
# Запустить тесты
./test-victoria-metrics.sh

# С кастомным хостом
VICTORIA_METRICS_HOST=localhost ./test-victoria-metrics.sh
```

Скрипт проверит:

1. ✅ Доступность Victoria Metrics
2. ✅ Работу прокси через backend
3. ✅ Отправку тестовых метрик
4. ✅ Получение данных через API
5. ✅ Список доступных метрик

## 📊 Примеры запросов

### Для конкретного ирригатора

```promql
# Автоматически генерируется для каждого ирригатора
avg by (topic)(esp32_A8A154_value{
  topic=~"esp32_A8A154/state/outputs/Valve[2]",
  tag="outputs"
})
```

### Дополнительные запросы

```promql
# Все клапаны устройства
esp32_A8A154_value{topic=~"esp32_.*Valve.*"}

# Количество переключений
changes(esp32_A8A154_value[1h])

# Время работы (сумма)
sum_over_time(esp32_A8A154_value[24h])
```

## 🐛 Troubleshooting

### Ошибка: "API не найден"

Проблема: Backend возвращает HTML вместо JSON

**Решение:**

1. Проверьте что backend запущен: `npm run server`
2. Проверьте логи backend - должно быть:
   ```
   [Victoria Metrics] Proxying request: http://ddweed.org:8428/...
   ```
3. Убедитесь что Victoria Metrics доступен:
   ```bash
   curl http://ddweed.org:8428/health
   ```

### Нет данных на графике

**Решение:**

1. Проверьте что метрики отправляются в Victoria Metrics
2. Проверьте запрос - он должен соответствовать меткам метрик
3. Проверьте временной диапазон
4. Запустите тестовый скрипт: `./test-victoria-metrics.sh`

### CORS ошибки

Backend автоматически проксирует запросы, CORS ошибок быть не должно.

Если они есть - проверьте что используется правильный URL: `/api/v1/query_range`

## 📝 Кастомизация

### Добавить график в другой компонент

```jsx
import MetricsChart from "../MetricsChart";

function MyComponent() {
  return (
    <MetricsChart
      title="Моя метрика"
      query='my_metric{label="value"}'
      victoriaMetricsUrl="/api/v1/query_range"
      defaultTimeRange="1h"
      autoRefresh={true}
      refreshInterval="30s"
      height={400}
    />
  );
}
```

### Изменить запрос для ирригатора

Отредактируйте в [IrrigatorCard.jsx](../client-vite/src/components/DeviceCard/IrrigatorCard.jsx):

```jsx
const metricsQuery = `your_custom_query{irrigator="${irrigatorNumber}"}`;
```

## 📚 Дополнительно

- [Полная документация Victoria Metrics](VICTORIA_METRICS.md)
- [Документация компонента](client-vite/src/components/MetricsChart/README.md)
- [Примеры использования](client-vite/src/components/MetricsChart/MetricsChart.example.jsx)

## 🎯 Что дальше?

1. **Настройте отправку метрик** с ваших ESP32 устройств в Victoria Metrics
2. **Добавьте дополнительные метрики**: температура, влажность, pH и т.д.
3. **Создайте дашборды** с несколькими графиками
4. **Настройте алерты** на аномальные значения
