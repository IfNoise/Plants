import { Box } from '@mui/material';
import React from 'react';

const Dashboard = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}>
      <h1>Stat Page</h1>
      </Box>
  );
};

export default Dashboard;
